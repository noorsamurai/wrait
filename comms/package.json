{
  "name": "wrait-comms",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-platform office messenger for Windows, macOS and iOS",
  "scripts": {
    "server": "node --experimental-sqlite apps/server/src/index.js",
    "dev": "pnpm --filter @comms/desktop dev",
    "build": "pnpm --filter @comms/desktop build",
    "typecheck": "pnpm --filter @comms/desktop exec tsc --noEmit",
    "test": "node --experimental-sqlite --test apps/server/test/relay.test.js",
    "e2e": "pnpm --filter @comms/desktop exec playwright test messaging"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
